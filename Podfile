# Uncomment the next line to define a global platform for your project
platform :ios, '9.0'

def shared_pods
    pod 'PhoneNumberKit', '~> 2.1'
    pod 'SnapKit', '~> 4.0.0'
end

target 'CountryPickerTextfield-iOS' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for CountryPickerTextfield-iOS
  shared_pods
  
end

target 'CountryPickerTextFieldFramework' do
    
    use_frameworks!
    shared_pods
    
end
